## What & why

<!-- What does this change and why? Link the issue it addresses (e.g. Closes #12). -->

## How it was tested

<!-- Commands run, cases covered. -->

- [ ] `cargo fmt --all --check` and `cargo clippy -p studdup-core --all-targets -- -D warnings` pass
- [ ] `cargo test` passes
- [ ] `npm --prefix ui run build` and `npm --prefix ui run test` pass
- [ ] Added/updated tests for the behaviour changed
- [ ] Commits follow Conventional Commits

## Notes

<!-- Anything reviewers should know: trade-offs, follow-ups, screenshots. -->
